// Тестовые данные для приложения

export interface User {
  id: string;
  name: string;
  email: string;
  avatar?: string;
}

export interface Property {
  id: string;
  title: string;
  description: string;
  address: string;
  city: string;
  state: string;
  metro?: string; // станция метро
  price: number; // цена за месяц (для аренды) или цена покупки в миллионах
  priceDaily?: number; // цена посуточно
  priceBuy?: number; // цена покупки в миллионах рублей
  type: 'rent' | 'buy';
  beds: number;
  baths: number;
  squareFeet: number;
  images: string[]; // эмодзи вместо фото
  latitude?: number;
  longitude?: number;
  facilities: string[];
  ownerId: string;
  available: boolean;
  featured?: boolean;
}

export interface Booking {
  id: string;
  propertyId: string;
  userId: string;
  date: string;
  adults: number;
  status: 'pending' | 'confirmed' | 'cancelled';
  rentType?: 'monthly' | 'daily';
}

export interface Purchase {
  id: string;
  propertyId: string;
  userId: string;
  price: number;
  status: 'pending' | 'paid';
  createdAt: string;
}

export interface Chat {
  id: string;
  userId: string;
  propertyId?: string;
  messages: Message[];
  unread: number;
}

export interface Message {
  id: string;
  text: string;
  senderId: string;
  timestamp: string;
}

// Начальные тестовые данные
export const initialUsers: User[] = [
  {
    id: '1',
    name: 'Иван Петров',
    email: 'ivan.petrov@mail.ru',
  },
  {
    id: '2',
    name: 'Мария Смирнова',
    email: 'maria.smirnova@mail.ru',
  },
];

// Российские города с метро и координатами
const russianCities = [
  { city: 'Москва', state: 'Московская область, Россия', lat: 55.7558, lng: 37.6173, metros: ['Тверская', 'Арбатская', 'Лубянка', 'Комсомольская', 'Университет', 'Кропоткинская', 'Парк Культуры', 'Фрунзенская', 'Спортивная', 'Лужники', 'Воробьевы горы', 'Киевская', 'Студенческая', 'Кутузовская', 'Фили', 'Багратионовская', 'Филевский парк', 'Пионерская', 'Кунцевская', 'Молодежная'] },
  { city: 'Санкт-Петербург', state: 'Ленинградская область, Россия', lat: 59.9343, lng: 30.3351, metros: ['Невский проспект', 'Адмиралтейская', 'Проспект Просвещения', 'Площадь Восстания', 'Василеостровская', 'Приморская', 'Черная речка', 'Петроградская', 'Горьковская', 'Невский проспект', 'Сенная площадь', 'Технологический институт', 'Фрунзенская', 'Московские ворота', 'Электросила', 'Парк Победы', 'Московская', 'Звездная', 'Купчино'] },
  { city: 'Казань', state: 'Республика Татарстан, Россия', lat: 55.7986, lng: 49.1064, metros: ['Кремлевская', 'Площадь Тукая', 'Суконная слобода', 'Аметьево', 'Горки', 'Проспект Победы'] },
  { city: 'Екатеринбург', state: 'Свердловская область, Россия', lat: 56.8431, lng: 60.6454, metros: ['Геологическая', 'Чкаловская', 'Ботаническая', 'Уральская', 'Машиностроителей', 'Уралмаш', 'Проспект Космонавтов', 'Бажовская'] },
  { city: 'Новосибирск', state: 'Новосибирская область, Россия', lat: 55.0084, lng: 82.9357, metros: ['Площадь Ленина', 'Октябрьская', 'Речной вокзал', 'Студенческая', 'Площадь Маркса', 'Заельцовская', 'Гагаринская', 'Красный проспект'] },
  { city: 'Нижний Новгород', state: 'Нижегородская область, Россия', lat: 56.3269, lng: 44.0075, metros: ['Горьковская', 'Московская', 'Чкаловская', 'Ленинская', 'Заречная', 'Двигатель Революции', 'Пролетарская', 'Автозаводская'] },
  { city: 'Краснодар', state: 'Краснодарский край, Россия', lat: 45.0355, lng: 38.9753, metros: [] },
  { city: 'Сочи', state: 'Краснодарский край, Россия', lat: 43.6028, lng: 39.7302, metros: [] },
  { city: 'Ростов-на-Дону', state: 'Ростовская область, Россия', lat: 47.2357, lng: 39.7015, metros: [] },
  { city: 'Уфа', state: 'Республика Башкортостан, Россия', lat: 54.7431, lng: 55.9678, metros: [] },
  { city: 'Самара', state: 'Самарская область, Россия', lat: 53.2001, lng: 50.15, metros: [] },
  { city: 'Омск', state: 'Омская область, Россия', lat: 54.9885, lng: 73.3242, metros: [] },
  { city: 'Челябинск', state: 'Челябинская область, Россия', lat: 55.1644, lng: 61.4368, metros: [] },
  { city: 'Воронеж', state: 'Воронежская область, Россия', lat: 51.6720, lng: 39.1843, metros: [] },
  { city: 'Пермь', state: 'Пермский край, Россия', lat: 58.0105, lng: 56.2502, metros: [] },
];

// Улицы для каждого города
const moscowStreets = ['Тверская', 'Арбат', 'Лубянка', 'Комсомольская', 'Ленинский проспект', 'Красная площадь', 'Кутузовский проспект', 'Ломоносовский проспект', 'Воробьевы горы', 'Проспект Мира', 'Тверской бульвар', 'Никитский бульвар', 'Гоголевский бульвар', 'Пречистенка', 'Остоженка', 'Пятницкая', 'Большая Полянка', 'Большая Ордынка', 'Якиманка', 'Берсеневская набережная'];
const spbStreets = ['Невский проспект', 'наб. реки Мойки', 'наб. канала Грибоедова', 'Большой проспект В.О.', 'Средний проспект В.О.', 'Малая Морская', 'Большая Морская', 'Гороховая', 'Садовая', 'Лиговский проспект', 'Московский проспект', 'пр. Энгельса', 'пр. Просвещения', 'пр. Культуры', 'пр. Славы', 'ул. Рубинштейна', 'ул. Ломоносова', 'ул. Пестеля', 'ул. Чайковского', 'ул. Радищева'];
const kazanStreets = ['ул. Баумана', 'ул. Кремлевская', 'ул. Пушкина', 'ул. Карла Маркса', 'ул. Декабристов', 'ул. Гагарина', 'пр. Победы', 'ул. Чистопольская', 'ул. Адоратского', 'ул. Вишневского'];
const ekbStreets = ['ул. Ленина', 'ул. Малышева', 'ул. Вайнера', 'ул. Карла Либкнехта', 'пр. Ленина', 'ул. Сакко и Ванцетти', 'ул. Радищева', 'ул. Тургенева', 'ул. Чапаева', 'ул. Луначарского'];
const novosibStreets = ['Красный проспект', 'ул. Ленина', 'ул. Гоголя', 'ул. Каменская', 'ул. Вокзальная магистраль', 'ул. Богдана Хмельницкого', 'ул. Добролюбова', 'ул. Сибревкома', 'ул. Фрунзе', 'ул. Советская'];
const nizhnyStreets = ['ул. Покровка', 'ул. Большая Покровская', 'ул. Рождественская', 'ул. Ильинская', 'ул. Варварская', 'ул. Минина и Пожарского', 'ул. Пискунова', 'ул. Алексеевская', 'ул. Семашко', 'ул. Белинского'];
const otherStreets = ['ул. Ленина', 'ул. Советская', 'ул. Мира', 'ул. Пушкина', 'ул. Гагарина', 'пр. Победы', 'ул. Центральная', 'ул. Первомайская', 'ул. Октябрьская', 'ул. Комсомольская'];

// Простая детерминированная функция для псевдослучайных чисел
function seededRandom(seed: number): number {
  const x = Math.sin(seed) * 10000;
  return x - Math.floor(x);
}

// Генерация большого массива объектов
function generateProperties(): Property[] {
  const properties: Property[] = [];
  const emojis = ['🏠', '🏡', '🏢', '🏘️', '🛋️', '🍳', '🛏️', '🚿', '🌳', '🌊', '🏙️'];
  const facilities = ['Wi-Fi', 'Парковка', 'Кондиционер', 'Стиральная машина', 'Балкон', 'Лифт', 'Сад', 'Гараж', 'Вид на реку', 'Детская площадка'];
  
  let idCounter = 1;
  let globalSeed = 12345; // Фиксированный seed для детерминированности
  
  russianCities.forEach((cityData, cityIndex) => {
    const streets = cityData.city === 'Москва' ? moscowStreets :
                   cityData.city === 'Санкт-Петербург' ? spbStreets :
                   cityData.city === 'Казань' ? kazanStreets :
                   cityData.city === 'Екатеринбург' ? ekbStreets :
                   cityData.city === 'Новосибирск' ? novosibStreets :
                   cityData.city === 'Нижний Новгород' ? nizhnyStreets :
                   otherStreets;
    
    const metroStations = cityData.metros.length > 0 ? cityData.metros : [cityData.city];
    
    // Генерируем разное количество квартир для каждого города (аренда)
    const propertiesPerCity = cityData.city === 'Москва' ? 50 :
                              cityData.city === 'Санкт-Петербург' ? 45 :
                              cityData.city === 'Казань' ? 15 :
                              cityData.city === 'Екатеринбург' ? 12 :
                              cityData.city === 'Новосибирск' ? 12 :
                              cityData.city === 'Нижний Новгород' ? 10 :
                              6;
    
    // Генерируем объекты на продажу (увеличено количество)
    const buyPropertiesPerCity = cityData.city === 'Москва' ? 80 :
                                cityData.city === 'Санкт-Петербург' ? 70 :
                                cityData.city === 'Казань' ? 30 :
                                cityData.city === 'Екатеринбург' ? 25 :
                                cityData.city === 'Новосибирск' ? 25 :
                                cityData.city === 'Нижний Новгород' ? 20 :
                                15;
    
    for (let i = 0; i < propertiesPerCity; i++) {
      const seed = globalSeed + cityIndex * 1000 + i * 17;
      const r1 = seededRandom(seed);
      const r2 = seededRandom(seed + 1);
      const r3 = seededRandom(seed + 2);
      const r4 = seededRandom(seed + 3);
      const r5 = seededRandom(seed + 4);
      const r6 = seededRandom(seed + 5);
      
      const street = streets[Math.floor(r1 * streets.length)];
      const metro = metroStations[Math.floor(r2 * metroStations.length)];
      const houseNum = Math.floor(r3 * 200) + 1;
      const floor = Math.floor(r4 * 20) + 1;
      const totalFloors = Math.floor(r5 * 15) + floor;
      
      // Типы квартир
      const roomTypes = [
        { rooms: 1, sqMin: 28, sqMax: 40, beds: 1, baths: 1 },
        { rooms: 2, sqMin: 50, sqMax: 70, beds: 2, baths: 1 },
        { rooms: 3, sqMin: 75, sqMax: 100, beds: 3, baths: 2 },
        { rooms: 4, sqMin: 110, sqMax: 140, beds: 4, baths: 2 },
      ];
      
      const roomType = roomTypes[Math.floor(r6 * roomTypes.length)];
      const squareFeet = Math.floor(r1 * (roomType.sqMax - roomType.sqMin + 1)) + roomType.sqMin;
      
      // Реальные цены в зависимости от города и типа
      let basePrice = cityData.city === 'Москва' ? 45000 :
                     cityData.city === 'Санкт-Петербург' ? 35000 :
                     cityData.city === 'Казань' ? 28000 :
                     cityData.city === 'Екатеринбург' ? 25000 :
                     cityData.city === 'Новосибирск' ? 22000 :
                     cityData.city === 'Нижний Новгород' ? 20000 :
                     18000;
      
      // Увеличиваем цену в зависимости от площади и комнат
      basePrice += (squareFeet - 30) * 300 + (roomType.rooms - 1) * 15000;
      
      // Добавляем вариацию (детерминированную)
      basePrice = Math.floor(basePrice * (0.8 + r2 * 0.4));
      
      // Ограничиваем диапазон
      basePrice = Math.max(25000, Math.min(150000, basePrice));
      
      // Цена посуточно (примерно 1/30 от месячной, но с вариацией)
      const priceDaily = Math.floor(basePrice / 30 * (0.7 + r3 * 0.6));
      const finalPriceDaily = Math.max(1500, Math.min(5000, priceDaily));
      
      const address = `${cityData.city}, ${street.includes('ул.') || street.includes('пр.') || street.includes('наб.') ? street : `ул. ${street}`}, д. ${houseNum}`;
      
      const title = roomType.rooms === 1 && squareFeet < 35 
        ? `Студия · ${squareFeet} м² · ${floor}/${totalFloors} этаж`
        : `${roomType.rooms}-комн. кв. · ${squareFeet} м² · ${floor}/${totalFloors} этаж`;
      
      const descriptions = [
        'Прекрасная квартира с современным ремонтом. Идеально подходит для комфортного проживания.',
        'Светлая и уютная квартира в тихом районе. Отличная транспортная доступность.',
        'Современная квартира в центре города с панорамными окнами.',
        'Просторная квартира в элитном районе с видом на парк.',
        'Стильная квартира с евроремонтом. Рядом парк, школа и магазины.',
        'Уютная квартира в новом доме с хорошей планировкой.',
        'Квартира с видом на реку в тихом районе.',
        'Просторная квартира в спальном районе с детской площадкой во дворе.',
        'Современная квартира с отличной инфраструктурой.',
        'Квартира в историческом центре с красивым видом.',
      ];
      
      const descIndex = Math.floor(r4 * descriptions.length);
      const facilitiesCount = Math.floor(r5 * 4) + 2;
      const emojisCount = Math.floor(r6 * 4) + 2;
      
      // Детерминированный выбор удобств и эмодзи
      const selectedFacilities: string[] = [];
      const selectedEmojis: string[] = [];
      
      for (let j = 0; j < facilitiesCount && j < facilities.length; j++) {
        const idx = Math.floor(seededRandom(seed + 10 + j) * facilities.length);
        if (!selectedFacilities.includes(facilities[idx])) {
          selectedFacilities.push(facilities[idx]);
        }
      }
      
      for (let j = 0; j < emojisCount && j < emojis.length; j++) {
        const idx = Math.floor(seededRandom(seed + 20 + j) * emojis.length);
        if (!selectedEmojis.includes(emojis[idx])) {
          selectedEmojis.push(emojis[idx]);
        }
      }
      
      // Небольшое смещение координат для разнообразия (детерминированное)
      const latOffset = (r1 - 0.5) * 0.1;
      const lngOffset = (r2 - 0.5) * 0.1;
      
      properties.push({
        id: idCounter.toString(),
        title,
        description: descriptions[descIndex],
        address,
        city: cityData.city,
        state: cityData.state,
        metro: metro,
        price: basePrice,
        priceDaily: finalPriceDaily,
        type: 'rent',
        beds: roomType.beds,
        baths: roomType.baths,
        squareFeet,
        images: selectedEmojis.length > 0 ? selectedEmojis : ['🏠'],
        latitude: cityData.lat + latOffset,
        longitude: cityData.lng + lngOffset,
        facilities: selectedFacilities.length > 0 ? selectedFacilities : ['Wi-Fi', 'Парковка'],
        ownerId: '2',
        available: true,
        featured: idCounter <= 20, // Первые 20 - рекомендуемые
      });
      
      idCounter++;
      globalSeed += 37; // Увеличиваем seed для следующей итерации
    }
    
    // Генерируем объекты на продажу
    for (let i = 0; i < buyPropertiesPerCity; i++) {
      const seed = globalSeed + cityIndex * 2000 + i * 23;
      const r1 = seededRandom(seed);
      const r2 = seededRandom(seed + 1);
      const r3 = seededRandom(seed + 2);
      const r4 = seededRandom(seed + 3);
      const r5 = seededRandom(seed + 4);
      const r6 = seededRandom(seed + 5);
      
      const street = streets[Math.floor(r1 * streets.length)];
      const metro = metroStations[Math.floor(r2 * metroStations.length)];
      const houseNum = Math.floor(r3 * 200) + 1;
      const floor = Math.floor(r4 * 20) + 1;
      const totalFloors = Math.floor(r5 * 15) + floor;
      
      // Типы квартир для покупки (обычно больше и дороже)
      const roomTypes = [
        { rooms: 1, sqMin: 35, sqMax: 55, beds: 1, baths: 1 },
        { rooms: 2, sqMin: 60, sqMax: 90, beds: 2, baths: 1 },
        { rooms: 3, sqMin: 90, sqMax: 130, beds: 3, baths: 2 },
        { rooms: 4, sqMin: 130, sqMax: 200, beds: 4, baths: 2 },
        { rooms: 5, sqMin: 200, sqMax: 280, beds: 5, baths: 3 },
      ];
      
      const roomType = roomTypes[Math.floor(r6 * roomTypes.length)];
      const squareFeet = Math.floor(r1 * (roomType.sqMax - roomType.sqMin + 1)) + roomType.sqMin;
      
      // Цена покупки в миллионах рублей (от 5 до 150 миллионов)
      // Зависит от города, площади и количества комнат
      let basePriceBuy = cityData.city === 'Москва' ? 25 :
                        cityData.city === 'Санкт-Петербург' ? 20 :
                        cityData.city === 'Казань' ? 12 :
                        cityData.city === 'Екатеринбург' ? 10 :
                        cityData.city === 'Новосибирск' ? 9 :
                        cityData.city === 'Нижний Новгород' ? 8 :
                        6;
      
      // Увеличиваем цену в зависимости от площади и комнат
      basePriceBuy += (squareFeet - 30) * 0.1 + (roomType.rooms - 1) * 8;
      
      // Добавляем вариацию (детерминированную) для большего разброса
      basePriceBuy = basePriceBuy * (0.5 + r2 * 1.0);
      
      // Ограничиваем диапазон от 5 до 150 миллионов
      basePriceBuy = Math.max(5, Math.min(150, basePriceBuy));
      
      // Округляем до 0.1 миллиона
      basePriceBuy = Math.round(basePriceBuy * 10) / 10;
      
      const address = `${cityData.city}, ${street.includes('ул.') || street.includes('пр.') || street.includes('наб.') ? street : `ул. ${street}`}, д. ${houseNum}`;
      
      const title = roomType.rooms === 1 && squareFeet < 40 
        ? `Студия · ${squareFeet} м² · ${floor}/${totalFloors} этаж`
        : `${roomType.rooms}-комн. кв. · ${squareFeet} м² · ${floor}/${totalFloors} этаж`;
      
      const descriptions = [
        'Элитная квартира с современным ремонтом премиум-класса. Идеально для постоянного проживания.',
        'Просторная квартира в престижном районе с панорамными окнами и видом на город.',
        'Роскошная квартира в центре города с дизайнерским ремонтом.',
        'Эксклюзивная квартира в элитном доме с закрытой территорией и охраной.',
        'Квартира бизнес-класса с современной планировкой и качественной отделкой.',
        'Просторная квартира в новостройке с панорамными окнами.',
        'Элитная квартира с видом на реку в тихом престижном районе.',
        'Квартира премиум-класса в историческом центре с высокими потолками.',
        'Современная квартира с отличной инфраструктурой и развитой транспортной доступностью.',
        'Эксклюзивная квартира с террасой и видом на парк.',
      ];
      
      const descIndex = Math.floor(r4 * descriptions.length);
      const facilitiesCount = Math.floor(r5 * 4) + 3; // Больше удобств для объектов на продажу
      const emojisCount = Math.floor(r6 * 4) + 3;
      
      // Детерминированный выбор удобств и эмодзи
      const selectedFacilities: string[] = [];
      const selectedEmojis: string[] = [];
      
      for (let j = 0; j < facilitiesCount && j < facilities.length; j++) {
        const idx = Math.floor(seededRandom(seed + 10 + j) * facilities.length);
        if (!selectedFacilities.includes(facilities[idx])) {
          selectedFacilities.push(facilities[idx]);
        }
      }
      
      for (let j = 0; j < emojisCount && j < emojis.length; j++) {
        const idx = Math.floor(seededRandom(seed + 20 + j) * emojis.length);
        if (!selectedEmojis.includes(emojis[idx])) {
          selectedEmojis.push(emojis[idx]);
        }
      }
      
      // Небольшое смещение координат для разнообразия (детерминированное)
      const latOffset = (r1 - 0.5) * 0.1;
      const lngOffset = (r2 - 0.5) * 0.1;
      
      properties.push({
        id: idCounter.toString(),
        title,
        description: descriptions[descIndex],
        address,
        city: cityData.city,
        state: cityData.state,
        metro: metro,
        price: basePriceBuy, // Цена в миллионах для покупки
        priceBuy: basePriceBuy, // Дублируем для ясности
        type: 'buy',
        beds: roomType.beds,
        baths: roomType.baths,
        squareFeet,
        images: selectedEmojis.length > 0 ? selectedEmojis : ['🏠'],
        latitude: cityData.lat + latOffset,
        longitude: cityData.lng + lngOffset,
        facilities: selectedFacilities.length > 0 ? selectedFacilities : ['Wi-Fi', 'Парковка', 'Кондиционер'],
        ownerId: '2',
        available: true,
        // Больше рекомендуемых квартир на покупку - каждая 8-я из первых 500
        featured: idCounter <= 500 && (idCounter % 8 === 0),
      });
      
      idCounter++;
      globalSeed += 41; // Увеличиваем seed для следующей итерации
    }
  });
  
  return properties;
}

export const initialProperties: Property[] = generateProperties();

export const initialChats: Chat[] = [
  {
    id: '1',
    userId: '2',
    propertyId: '1',
    messages: [
      {
        id: '1',
        text: 'Здравствуйте! Интересует ваша квартира на Тверской. Можно ли посмотреть в выходные?',
        senderId: '1',
        timestamp: new Date(Date.now() - 3600000).toISOString(),
      },
      {
        id: '2',
        text: 'Добрый день! Да, конечно. В субботу в 14:00 будет удобно?',
        senderId: '2',
        timestamp: new Date(Date.now() - 1800000).toISOString(),
      },
      {
        id: '3',
        text: 'Отлично, подходит! Напишу за день до встречи для подтверждения.',
        senderId: '1',
        timestamp: new Date(Date.now() - 900000).toISOString(),
      },
    ],
    unread: 0,
  },
];
